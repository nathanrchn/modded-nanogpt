[package]
name = "fast_compress"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.38", features = ["derive"] }
tqdm = "0.7.0"
zip2zip_compression = { git = "https://github.com/epfl-dlab/zip2zip-compression", version = "0.3.3" }
